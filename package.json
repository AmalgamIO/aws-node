{
  "name": "init_session",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "build": "[[ -d dist ]] && rm -rf ./dist ; cp -r src dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "archimedon",
  "license": "ISC",
  "description": ""
}
