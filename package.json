{
  "name": "init_session",
  "version": "1.0.0",
  "main": "src/index.js",
  "author": "archimedon",
  "license": "ISC",
  "description": "",
  "private": true,
  "scripts": {
    "build": "[[ -d dist ]] && rm -rf ./dist ; cp -r src dist",
    "package": "[[ ! -d dist ]] && npm run build; cd dist && zip -r lambda.zip . && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
